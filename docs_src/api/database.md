# Database Configuration

The database configuration module handles database connections and session management.

::: config.database
    options:
      show_root_heading: true
      members:
        - validate_database_config
        - get_db
